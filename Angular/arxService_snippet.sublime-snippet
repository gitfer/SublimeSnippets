<snippet>
	<content><![CDATA[

angular.module('arxivar').factory('${1:this}', ['\$rootScope', '\$scope', '\$log', function (\$rootScope, \$scope, \$log) {
            
            ${4}

            return {
                ${2}:${3}
            };
}]);
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>arxs</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
